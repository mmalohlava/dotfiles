---
description: Implement next ready bead.
---

Your task is to implement next bead that is ready.

Execute the implementation in the following order (MUST): 
 - pick a ready task from `bd ready`.
 - implement the picked task. 
 - validate implementation by running tests. If anything fails, revisit implementation and fix failing test.
 - make sure that new code is covered by tests.
 - create a commit: Make sure that commit contains reference to the bead. Use conventional commit specification for commit message (https://www.conventionalcommits.org/en/v1.0.0/).
 - mark the picked bead done. 
